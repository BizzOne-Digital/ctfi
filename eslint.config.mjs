import { defineConfig, globalIgnores } from "eslint/config";
import nextVitals from "eslint-config-next/core-web-vitals";
import nextTs from "eslint-config-next/typescript";

const eslintConfig = defineConfig([
  ...nextVitals,
  ...nextTs,
  {
    rules: {
      // This app is a standard (non-React-Compiler) CSR admin dashboard that
      // intentionally fetches on mount and syncs local UI state from props
      // via useEffect — both are established, correct React patterns here.
      // The React Compiler lint rules treat that as an error by default;
      // downgrade to a warning rather than rewriting idiomatic effects.
      "react-hooks/set-state-in-effect": "warn",
    },
  },
  // Override default ignores of eslint-config-next.
  globalIgnores([
    // Default ignores of eslint-config-next:
    ".next/**",
    "out/**",
    "build/**",
    "next-env.d.ts",
  ]),
]);

export default eslintConfig;
